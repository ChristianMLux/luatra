{
  "name": "luatra-firestore-rules-test",
  "private": true,
  "type": "module",
  "description": "Checks firestore.rules against the queries the luatra apps actually run. Not part of the pnpm workspace.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-luatra --config ../firebase.json \"node --test rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
